apiVersion: v1
kind: Service
metadata:
  name: {{ include "home-inventory-frontend.fullname" . }}
  labels:
    {{- include "home-inventory-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "home-inventory-frontend.selectorLabels" . | nindent 4 }}
