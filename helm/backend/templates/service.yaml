apiVersion: v1
kind: Service
metadata:
  name: {{ include "home-inventory-backend.fullname" . }}
  labels:
    {{- include "home-inventory-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "home-inventory-backend.selectorLabels" . | nindent 4 }}
